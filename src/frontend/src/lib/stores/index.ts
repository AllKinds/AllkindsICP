//groups all stores
import { rootStore, regiStore, appStore } from './tasks/routing';
import { authStore, actor, user } from './tasks/auth';

export {
  authStore,
  actor,
  user,
  rootStore,
  regiStore,
  appStore
}